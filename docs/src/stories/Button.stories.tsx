import type { Meta, StoryObj } from '@storybook/react'
import { Button } from '@caju-ds/react'

export default {
  title: 'Form/Button',
  component: Button,
} as Meta

export const Primary: StoryObj = {}
